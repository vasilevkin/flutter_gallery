import 'package:flutter/material.dart';

class SimpleColorLoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Column(
        children: [],
      ),
    );
  }
}
